import React,{useState}from'react';import{createRoot}from'react-dom/client';import{createStore}from'redux';import{Provider,useDispatch,useSelector}from'react-redux';import'./style.css';
const reducer=(state=[],action)=>action.type==='ADD'? [...state,{id:Date.now(),text:action.text}]:state;const store=createStore(reducer);
function Todo(){const [text,setText]=useState('');const list=useSelector(s=>s);const dispatch=useDispatch();const add=e=>{e.preventDefault();if(text.trim()){dispatch({type:'ADD',text:text.trim()});setText('')}};return <main><h1>TODO list</h1><form onSubmit={add}><input value={text} onChange={e=>setText(e.target.value)} placeholder="Введіть завдання"/><button>Додати</button></form><ul>{list.map(x=><li key={x.id}>{x.text}</li>)}</ul><footer>Загальна кількість завдань: <b>{list.length}</b></footer></main>}createRoot(document.getElementById('root')).render(<Provider store={store}><Todo/></Provider>);
