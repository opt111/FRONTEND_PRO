import React, { useState } from 'react'; import { createRoot } from 'react-dom/client'; import './style.css'; const data = ['😀', '😊', '😎', '🤩', '😍']; function App() { const [v, setV] = useState([0, 0, 0, 0, 0]); return <main><h1>Голосування за смайлик</h1>{data.map((e, i) => <button className="emoji" onClick={() => setV(v.map((x, n) => n === i ? x + 1 : x))} key={e}>{e}<small>{v[i]}</small></button>)}</main> } createRoot(document.getElementById('root')).render(<App />);
